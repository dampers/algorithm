#include <bits/stdc++.h>
#define swap(a,b) (a)^=(b)^=(a)^=(b)
using namespace std;
typedef long long lld;
int gms[7];
int main()
{
	ios_base::sync_with_stdio(NULL);
	cin.tie(NULL);
	cout.tie(NULL);
	int n;
	gms[0] = 4;
	scanf("%d", &n);
	bool flag = false;
	int tmp = 4;
	while(n>tmp)
	{
		
	}
	printf("%d", tmp);
	return 0;
}
