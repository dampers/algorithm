#include <bits/stdc++.h>
#define swap(a,b) (a)^=(b)^=(a)^=(b)
using namespace std;
typedef long long lld;

char password[11];
char alp[27];

void f(int pos, int k)
{

}

int main()
{
	for(int i=0;i<26;i++)
		alp[i] = 'a'+i;
	int n;
	scanf("%d", &n);
	printf("%s", alp);
	return 0;
}