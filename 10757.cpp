#include <bits/stdc++.h>

using namespace std;
typedef long long lld;
char numa[10005], numb[10005];
int ans[10005];
int main()
{
	scanf("%s %s", numa, numb);
	int lena = strlen(numa), lenb = strlen(numb);
	
	return 0;
}